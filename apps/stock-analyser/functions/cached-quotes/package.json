{
  "name": "@transformotion/fn-cached-quotes",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"lint: cached-quotes\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@transformotion/contracts": "workspace:*",
    "@transformotion/lambda-middleware": "workspace:*"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3",
    "@aws-sdk/lib-dynamodb": "^3",
    "@types/aws-lambda": "^8",
    "typescript": "*"
  }
}
