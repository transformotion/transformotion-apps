import { TabStub } from './TabStub';

export function WatchlistPage() {
  return (
    <TabStub
      icon="⭐"
      label="Watchlist"
      description="Monitored stocks with cycle score alerts. Bulk refresh with a single click."
      phase="Phase 3 (S3.7)"
    />
  );
}
