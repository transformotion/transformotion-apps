{
  "name": "@transformotion/fn-invitations",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"lint: invitations\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@transformotion/lambda-middleware": "*"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3",
    "@aws-sdk/lib-dynamodb": "^3",
    "@aws-sdk/client-sesv2": "^3",
    "@types/aws-lambda": "^8",
    "typescript": "*"
  }
}
