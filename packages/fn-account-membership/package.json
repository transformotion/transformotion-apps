{
  "name": "@transformotion/fn-account-membership",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@transformotion/lambda-middleware": "workspace:*"
  },
  "devDependencies": {
    "@aws-sdk/lib-dynamodb": "^3",
    "@types/node": "^22",
    "typescript": "*",
    "vitest": "^1"
  }
}
