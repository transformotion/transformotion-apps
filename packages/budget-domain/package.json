{
  "name": "@transformotion/budget-domain",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"lint: budget-domain\"",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "^1.0.0"
  }
}
